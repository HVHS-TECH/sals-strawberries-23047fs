//Inactive
/*
{
    //Testing rules
    "rules": {
        ".read": true,
        ".write": true
    }
}
*/
//Active
{
    //Real rules
    "rules": {
        "users": {
            "$uid": {
                ".read": true,
                ".write": "auth.uid === $uid || auth.uid === 'pCmr0SLtPmOKwM0NYKmmow3QrtC2' || root.child('users').child(auth.uid).child('role').val() === 'admin'"
                //Allows school account and any account with admin role added
            }
        }
    }
}